name: CI

on:
  push:
    branches: [ '*' ]
  workflow_dispatch:



jobs:

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Successful compilation
        run: |
          make
          ./compile.sh test
          ./compile.sh release

  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Successful compilation
        run: |
          make
          ./compile.sh test
          ./compile.sh release
